# Задача 1
# Примечания: введенное число должно быть четное или оканчиваться на 5

x = int(input('Введенное число должно быть четное или оканчиваться на 5!\nВведите число: '))
while x % 5 != 0 and x % 2 != 0:
    x = int(input('Число не четное и не заканчивается на 5.\nВведите число еще раз: '))
else:
    if x % 5 == 0 and x % 2 == 0:
        print(f'Число "{x}" четное и заканчивается на 5.')
    elif x % 2 == 0:
        print(f'Число "{x}" четное, но не заканчивается на 5.')
    else:
        print(f'Число "{x}" не четное, но заканчивается на 5.')

# Ответ --->
# Зависит от введенного числа


# Задача 2
# Примечания: придумать другой код выполняющий ту же задачу
# i = 0
# while i < 10:
#     print(i)
#     i = i + 1

for i in range(10):
    print(i)

# Ответ --->
# Числа от 0 до 10


# Задача 3
# Примечания: сумма чисел от 12345 до 56789 (не включительно)

K = None
N = None
answer = None

while True:
    try:
        K = int(input('Введите первое положительное число "K": '))
        if K <= 0:
            print('Введенное число "K" не положительное!')
            continue
        else:
            while True:
                try:
                    N = int(input('Введите второе положительное число "N", которое должно быть больше "K": '))
                    if N <= 0:
                        print('Введенное число "K" не положительное!')
                    elif K >= N:
                        print('Введенное число "N" не меньше числа "K"!')
                    else:
                        answer = 0
                        for i in range(K, N + 1):
                            if i % 2 != 0:
                                answer += i
                        break
                except ValueError:
                    print("Некорректный ввод. Введите число!")
        break
    except ValueError:
        print("Некорректный ввод. Введите число!")

print(f'Сумма нечетных положительных чисел в диапазоне от "{K}" до "{N}" включительно составит "{answer}".')

# Ответ --->
# Сумма нечетных положительных чисел в диапазоне от "12345" до "56789" включительно составит "768182441".


# Задача 4
# Примечания: факториал числа 10

K = None
answer = None

while True:
    try:
        K = int(input('Введите положительное число "K": '))
        if K <= 0:
            print('Введенное число "K" не положительное!')
        else:
            answer = 1
            for i in range(1, K + 1):
                answer *= i
            break
    except ValueError:
        print("Некорректный ввод. Введите число!")

print(f'Факториал числа "{K}" равен "{answer}".')

# Ответ --->
# Факториал числа "10" равен "3628800".
