# Задача 1
# Примечания: ввести числа 3, 5, 4, 3

n = int(input())    # Ввод числа 3
a = [0]   # Создание кортежа с одним элементом 0
for i in range(n):      # Сгенерирует последовательность 0, 1, 2 по которой будет итерироваться цикл
    x = int(input())    # Ввод чисел 5, 4 и 3 на каждой итерации цикла по числам 0, 1 и 2 соответственно
    a = [i] * x
    # Цикл 1 - создается кортеж [0], который умножается на 5 - получаем [0, 0, 0, 0, 0]
    # Цикл 2 - создается кортеж [1], который умножается на 4 - получаем [1, 1, 1, 1]
    # Цикл 3 - создается кортеж [2], который умножается на 3 - получаем [2, 2, 2]
    # Предыдущее значение a = [0] заменяется в каждом цикле на новое, то есть последнее значение a = [2, 2, 2]
print(a)

# Ответ --->
# [2, 2, 2]


# Задача 2
# Примечания: ввести число 3

n = int(input())    # Ввод числа 3
a = []     # Создание пустого кортежа

for i in range(n):      # Сгенерирует последовательность 0, 1, 2 по которой будет итерироваться цикл
    a = a + [i] * i
    # Цикл 1 - к [] прибавляем кортеж [0], умноженный на 0 - получаем a = []
    # Цикл 2 - к [] прибавляем кортеж [1], умноженный на 1 - получаем a = [1]
    # Цикл 3 - к [1] прибавляем кортеж [2], умноженный на 2 - получаем [1, 2, 2]
print(a)

# Ответ --->
# [1, 2, 2]


# Задача 3
# Примечания: ввести число 5 и последовательность чисел 2, 3, 1, 5, 10

n = int(input())    # Ввод числа 5
a = []     # Создание пустого кортежа
summ_element = 0    # Создание пустой переменной

for i in range(n):      # Сгенерирует n итераций цикла (в примере их 5)
    m = int(input())    # Потребуется ввести числа (в примере это числа 2, 3, 1, 5 и 10)
    a = a + [m]     # Введенные числа будут последовательно добавлены в кортеж a
    # Цикл 1 - к [] прибавляем кортеж [2] - получаем a = [2]
    # Цикл 2 - к [2] прибавляем кортеж [3] - получаем a = [2, 3]
    # Цикл 3 - к [2, 3] прибавляем кортеж [1] - получаем a = [2, 3, 1]
    # Цикл 4 - к [2, 3, 1] прибавляем кортеж [5] - получаем a = [2, 3, 1, 5]
    # Цикл 5 - к [2, 3, 1, 5] прибавляем кортеж [10] - получаем a = [2, 3, 1, 5, 10]
    summ_element += m
    # Суммируем все введенные числа

print(a)
print(summ_element / n)

# Ответ --->
# 4.2


# Задача 4

n = int(input('Введите количество чисел массива: '))
a = []

for i in range(n):
    element = int(input(f'Введите {i+1} число массива: '))
    a = a + [element]
print(f'Массив: {a}')

m = int(input('Введите индекс требуемого числа массива: '))
element_index = a[m]
print(f'Число массива с индексом "{m}" равно: "{element_index}"')
